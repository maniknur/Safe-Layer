# =============================================================================
# SafeLayer BNB — Environment Variables
# Copy this file to the appropriate location and fill in your values.
# =============================================================================

# ── Backend (.env in /backend) ──────────────────────────────────────────────

# Server
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Rate limiting
RATE_LIMIT_MAX=30

# BscScan API (get yours at https://bscscan.com/apis)
BSCSCAN_API_KEY=

# BNB Chain RPC
BNB_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545/

# On-Chain Registry
REGISTRY_CONTRACT_ADDRESS=0x20B28a7b961a6d82222150905b0C01256607B5A3
ANALYZER_PRIVATE_KEY=

# Database (optional — not required for MVP)
DATABASE_URL=postgresql://user:password@localhost:5432/safelayer_bnb

# ── Frontend (.env.local in /frontend) ──────────────────────────────────────

NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
NEXT_PUBLIC_BNB_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545/

# ── Smart Contracts (.env in /smart-contracts) ──────────────────────────────

DEPLOYER_PRIVATE_KEY=
BNBTESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545/
# BSCSCAN_API_KEY= (same key as backend, shared)
