## =============================================================================
## BACKEND ENVIRONMENT VARIABLES (.env for /backend)
## =============================================================================

# Server Configuration
PORT=3001
NODE_ENV=development
LOG_LEVEL=info

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Database (Optional - currently uses mock data)
DATABASE_URL=postgresql://user:password@localhost:5432/safelayer_bnb
DB_POOL_SIZE=10

# Blockchain RPC
BNB_RPC_URL=https://bsc-dataseed.binance.org/
BNB_RPC_TIMEOUT=30000

# AI Configuration
AI_MODEL=rule_based
EXPLANATION_DETAIL_LEVEL=detailed

## =============================================================================
## FRONTEND ENVIRONMENT VARIABLES (.env.local for /frontend)
## =============================================================================

NEXT_PUBLIC_BACKEND_URL=http://localhost:3001
NEXT_PUBLIC_ANALYTICS_ID=
NEXT_PUBLIC_BNB_RPC_URL=https://bsc-dataseed.binance.org/
